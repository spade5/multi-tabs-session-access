name: publish page

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 14
      - name: install and build
        run: |
          npm ci
          npm run build
        working-directory: ./example
      - name: publish page
        run: |
          git config user.email "spade5@spade5.name"
          git config user.name "spade5"
          npm run page
        working-directory: ./example